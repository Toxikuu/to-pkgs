#!/to

n="groff"
v="1.23.0"
a="Programs for processing and formatting text and images"
m="Tox"
l="GPL-3.0-or-later"
t="lib"
u="https://https.git.savannah.gnu.org/git/$n.git"
vf="gr | grep -E '^[0-9]+' | grep -Ev '[a-z]+'"

s=(
    "https://mirror.us-midwest-1.nexcess.net/gnu/$n/$n-$v.tar.gz"
)

d=(
    "perl"
    # "uchardet" # TODO: Add back support for this once I get cmake working
)

b() {

export PAGE=letter
def

}
