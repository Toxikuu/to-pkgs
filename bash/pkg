#!/to

n="bash"
v="5.2.37"
a="The Bourne-Again Shell"
m="Tox"
l="GPL-3.0-or-later"
t="shell core"
u="https://ftp.gnu.org/gnu/$n/?C=M;O=D"
# TODO: Make a more stable / less fragile vf
vf="curl -s '$u' | grep '$n-' | grep -Ev -- '-beta|-alpha|-rc|diff|doc|\.sig|patches' | head -n1 | cut -d- -f2 | cut -dt -f1 | sed 's,.$,,'"

s=(
    "https://ftp.gnu.org/gnu/$n/$n-$v.tar.gz"
)

d=(
    "glibc"
    "ncurses"
    "readline"
)

b() {

_cfg=(
    --without-bash-malloc
    --with-installed-readline
)

il
def

}
