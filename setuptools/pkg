#!/to

n="setuptools"
v="80.7.0"
a="Tool for managing Python packages"
m="Tox"
l="MIT"
t="py utils core lib"
u="https://github.com/pypa/$n.git"

s=(
    "${u%.git}/archive/refs/tags/v$v.tar.gz"
)

d=(
    "python"
    "wheel"
    "setuptools"
)

b() {

with pip

# Hotfix for `dist_info` build failure
pip install setuptools wheel

pipb
pipi

}
