#!/to

n="shared-mime-info"
v="2.4"
a="MIME database"
m="Tox"
l="GPL2"
t="db lib utils"
u="https://gitlab.freedesktop.org/xdg/$n.git"
vf="gr | grep -v Release-"

s=(
    "${u%.git}/-/archive/$v/$n-$v.tar.gz"
)

d=(
    "glibc"
    "glib"
    "libxml2"
    "b,meson"
    "b,ninja"
)

b() {

with mn
def

}

# I don't know how the fuck this fails, considering glib and libxml2 are listed
# as dependencies and should be installed, but arent ???????
# FIX: ^
#
# i() {
#
# update-mime-database /usr/share/mime
#
# }
