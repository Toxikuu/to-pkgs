#!/to

# NOTE: Mirrors are used because upstream is unstable
n="automake"
v="1.18"
a="Automatic Makefile generator"
m="Tox"
l="GPL"
t="utils core"
u="https://github.com/sailfishos-mirror/$n.git"
vf="gr | vfs | sed 's,^v,,' | grep -v '[a-Z]'"

s="https://mirrors.ocf.berkeley.edu/gnu/$n/$n-$v.tar.xz"

d=(
    "b,autoconf"
)

b() {

def

}
